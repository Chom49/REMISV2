<IfModule mod_rewrite.c>
    RewriteEngine On

    # Skip if already routed into public/
    RewriteCond %{REQUEST_URI} !^/[^/]*/public/
    RewriteCond %{REQUEST_URI} !^/[^/]*/public$

    # Route everything else into public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
